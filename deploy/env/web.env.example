# Production builds should inject runtime/session auth through the deployment
# platform. Do not commit long-lived user or service tokens here.
VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_API_AUTH_TOKEN=replace-with-dev-or-session-token
VITE_API_USER_ID=local-user
VITE_API_PROJECT_ID=default
